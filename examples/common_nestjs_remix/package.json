{
  "name": "common_nestjs_remix",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev-social": "turbo dev-social",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "generate:client": "pnpm run --filter=guidebook-web-app generate:client",
    "db:generate": "pnpm run --filter=api db:generate",
    "db:migrate": "pnpm run --filter=api db:migrate",
    "test:web": "pnpm run --filter=guidebook-web-app test",
    "test:api": "pnpm run --filter=api test",
    "test:web:e2e": "pnpm run --filter=guidebook-web-app test:e2e",
    "test:api:e2e": "pnpm run --filter=api test:e2e"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "^2.0.6"
  },
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=24.8.0"
  }
}
